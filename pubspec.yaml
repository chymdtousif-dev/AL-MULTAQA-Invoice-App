name: al_multqa_invoice
description: Invoice app for AL MULTAQA SMITHERY & ALUMINIUM WORKSHOP L.L.C.-O.P.C.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  pdf: ^3.11.3
  printing: ^5.14.2
  share_plus: ^11.0.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets: 
   - assets/logo.png 
  
